apiVersion: v1
kind: Service
metadata:
  name: {{ include "chocolate.fullname" . }}
  labels:
    {{- include "chocolate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chocolate.selectorLabels" . | nindent 4 }}
