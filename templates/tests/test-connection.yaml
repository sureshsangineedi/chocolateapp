apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chocolate.fullname" . }}-test-connection"
  labels:
    {{- include "chocolate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chocolate.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
